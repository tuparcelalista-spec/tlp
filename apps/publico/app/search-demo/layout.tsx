import type { ReactNode } from "react";

export const metadata = {
  title: "TPL · SearchWidget (herramienta interna, Bloque 2.4)",
  robots: { index: false, follow: false },
};

export default function SearchDemoLayout({ children }: { children: ReactNode }) {
  return children;
}
