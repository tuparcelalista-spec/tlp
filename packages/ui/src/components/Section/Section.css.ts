export const sectionCss = `
.tpl-section { padding-block: var(--tpl-section-y); }
.tpl-section--canvas { background: var(--tpl-surface-canvas); color: var(--tpl-content-primary); }
.tpl-section--raised { background: var(--tpl-surface-raised); color: var(--tpl-content-primary); }
.tpl-section--inverse { background: var(--tpl-surface-inverse); color: var(--tpl-content-inverse); }
.tpl-section--inverse :where(h1, h2, h3, h4) { color: var(--tpl-content-inverse); }
`;
