// Esqueleto de infraestructura — Fase 1.
// El AI Gateway real (Edge Function única de entrada + este paquete) se
// construye en el Prerequisito crítico A / Fase 12, después de cerrar el
// hallazgo de seguridad de veo-generar-video. No hay ninguna llamada a
// Gemini, Veo, ni a ninguna Edge Function existente en este esqueleto.
export const AI_GATEWAY_PLACEHOLDER = true;
